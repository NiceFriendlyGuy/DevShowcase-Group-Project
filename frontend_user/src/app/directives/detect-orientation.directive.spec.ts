import { DetectOrientationDirective } from './detect-orientation.directive';

describe('DetectOrientationDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectOrientationDirective();
    expect(directive).toBeTruthy();
  });
});
